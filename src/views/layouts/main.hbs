<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Innova Living - Muebles con Estilo</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&family=Playfair+Display:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --color-primary: #dc2626; /* Rojo principal */
            --color-primary-dark: #991b1b; /* Rojo oscuro */
            --color-primary-light: #fef2f2; /* Rojo muy claro */
            --color-secondary: #f59e0b; /* Amarillo/dorado */
            --color-text: #333; /* Gris oscuro para el texto */
            --color-text-light: #6b7280; /* Gris medio */
            --color-bg: #f0f2f5; /* Fondo gris claro */
            --color-card-bg: #ffffff; /* Fondo de tarjetas */
            --color-white: #ffffff;
            --color-border: #ddd; /* Borde gris claro */
            --color-shadow: rgba(0, 0, 0, 0.15); /* Sombra más suave */
            --font-family-primary: 'Montserrat', sans-serif;
            --font-family-secondary: 'Playfair Display', serif; /* Fuente elegante para títulos */
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: var(--font-family-primary);
            background-color: var(--color-bg);
            min-height: 100vh;
            color: var(--color-text);
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
        }

        /* Header con botón del sidebar */
        .header {
            background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
            color: var(--color-white);
            padding: 1.25rem 2.5rem; /* Aumenta el padding */
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15); /* Sombra más marcada */
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .header-content {
            display: flex;
            justify-content: center;
            align-items: center;
            max-width: 1400px;
            margin: 0 auto;
            position: relative;
        }

        .logo {
            font-size: 2rem; /* Tamaño más grande */
            font-weight: 700;
            color: var(--color-white);
            margin: 0;
            font-family: var(--font-family-secondary); /* Fuente secundaria para el logo */
            text-transform: uppercase; /* Texto en mayúsculas */
            letter-spacing: 2px; /* Espaciado entre letras */
        }

        /* Contenido principal */
        .main-container {
            max-width: 1400px;
            margin: 2rem auto;
            padding: 20px;
        }

        .content {
            background-color: var(--color-card-bg);
            border-radius: 12px; /* Bordes más suaves */
            box-shadow: 0 4px 25px var(--color-shadow); /* Sombra mejorada */
            padding: 3rem 4rem; /* Más espacio interior */
            min-height: 600px;
            position: relative;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        
        .content::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 6px; /* Borde superior más grueso */
            background: linear-gradient(90deg, var(--color-primary) 0%, var(--color-secondary) 100%);
            border-radius: 12px 12px 0 0; /* Bordes redondeados */
        }

        .content img {
            max-width: 100%;
            height: auto;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .content h2 {
            font-family: var(--font-family-secondary); /* Fuente secundaria para títulos */
            color: var(--color-primary);
            margin-bottom: 1.5rem; /* Espaciado ajustado */
            font-size: 2.5rem; /* Más grande */
            font-weight: 700;
            text-align: center;
            letter-spacing: 1px; /* Para un look más profesional */
        }

        .content p {
            color: var(--color-text-light);
            font-size: 1.1rem;
            line-height: 1.8; /* Más espaciado entre líneas */
            margin-bottom: 1rem;
            text-align: center;
        }

        /* --- ESTILOS PARA FORMULARIOS --- */
        .form-container {
            max-width: 600px; /* Ancho más generoso */
            width: 100%;
            margin: 0 auto;
        }
        
        .form-title {
            font-family: var(--font-family-secondary);
            font-size: 2.2rem;
            color: var(--color-primary-dark);
            text-align: center;
            margin-bottom: 2rem;
        }

        .content label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 600;
            color: var(--color-text);
            text-align: left;
            font-size: 1rem;
        }

        .content input[type="text"],
        .content input[type="number"],
        .content input[type="date"],
        .content input[type="email"],
        .content input[type="password"],
        .content select,
        .content textarea {
            width: 100%;
            padding: 14px 18px; /* Más padding para campos más grandes */
            margin-bottom: 1.5rem;
            border: 1px solid var(--color-border);
            border-radius: 8px; /* Bordes más redondeados */
            font-size: 1rem;
            font-family: var(--font-family-primary);
            box-sizing: border-box;
            background-color: #f8f9fa;
        }
        
        .content input:focus,
        .content select:focus,
        .content textarea:focus {
            outline: none;
            border-color: var(--color-primary);
            box-shadow: 0 0 0 4px var(--color-primary-light); /* Sombra de enfoque más marcada */
        }

        /* Botón de guardar */
        .btn-submit {
            display: block;
            width: 100%;
            padding: 1rem;
            background-color: var(--color-primary);
            color: var(--color-white);
            border: none;
            border-radius: 8px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s ease;
            margin-top: 1rem;
        }
        
        .btn-submit:hover {
            background-color: var(--color-primary-dark);
            transform: translateY(-3px);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        /* --- ESTILOS PARA LA NOTIFICACIÓN (TOAST) --- */
        #notification {
            position: fixed;
            top: 20px;
            left: -400px; /* Inicia fuera de la pantalla, un poco más lejos */
            width: 300px;
            min-height: 60px;
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 10px 20px;
            background-color: var(--color-card-bg); /* Fondo blanco de la tarjeta */
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            transition: left 0.6s cubic-bezier(0.68, -0.55, 0.27, 1.55); /* Animación elástica más distintiva */
            z-index: 99999;
        }

        #notification-bar {
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 8px;
            border-radius: 8px 0 0 8px;
        }

        #notification-icon {
            font-size: 2rem;
        }

        #notification-message {
            font-weight: 600;
            color: var(--color-text);
            flex-grow: 1;
            padding-right: 10px;
        }

        /* Clases para éxito y error */
        .notification-success .notification-bar {
            background-color: var(--color-primary); /* Rojo principal para éxito */
        }

        .notification-error .notification-bar {
            background-color: var(--color-primary-dark); /* Rojo oscuro para error */
        }

        /* Clase para mostrar la notificación con animación */
        .notification-show {
            left: 20px !important; /* Posición final dentro de la pantalla */
        }
        
        /* Clases para éxito y error */
        .notification-success .notification-bar {
            background-color: #28a745; /* Verde para éxito */
        }
        
        .notification-error .notification-bar {
            background-color: #dc3545; /* Rojo para error */
        }
        
    </style>
</head>
<body>
    <header class="header">
        <div class="header-content">
            <button class="menu-toggle" onclick="toggleSidebar()">☰</button>
            <h1 class="logo">Innova Living</h1>
        </div>
    </header>

    <nav class="navbar" id="navbar">
        <div class="navbar-header">
            <h2 class="navbar-title">Menú</h2>
            <button class="sidebar-close" onclick="toggleSidebar()">×</button>
        </div>
        <div class="navbar-nav">
            {{> navbar }}
        </div>
    {{#if user}}
  <li><a href="/logout">Cerrar sesión ({{user.nombreUsuario}})</a></li>
{{else}}
  <li><a href="/login">Iniciar sesión</a></li>
{{/if}}
</nav>
    
    <div class="sidebar-overlay" id="sidebarOverlay" onclick="toggleSidebar()"></div>

    <div class="main-container">
        <main class="content">
            {{#if isHomePage}}
                <img src="/Images/Inicio.webp" alt="Imagen de inicio de Innova Living" />
                <div>
                    <h2>Sistema de Ventas para Innova Living</h2>
{{#if user.administrador}}
                    <p>Gestiona tu inventario, clientes y ventas con estilo.</p>
{{/if}}
                </div>
            {{/if}}
            <div class="form-container">
                {{{body}}}
            </div>
        </main>
    </div>

    <div id="notification" class="notification-container">
        <div id="notification-bar"></div>
        <span id="notification-icon"></span>
        <p id="notification-message"></p>
    </div>

    <script>
        // Lógica del sidebar
        function toggleSidebar() {
            const sidebar = document.getElementById('navbar');
            const overlay = document.getElementById('sidebarOverlay');
            sidebar.classList.toggle('active');
            overlay.classList.toggle('active');
        }

        document.addEventListener('DOMContentLoaded', function() {
            const currentPath = window.location.pathname;
            const navLinks = document.querySelectorAll('.navbar-link');
            
            navLinks.forEach(link => {
                link.classList.remove('active');
                if (link.getAttribute('href') === currentPath) {
                    link.classList.add('active');
                }
            });
        });

        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape') {
                const sidebar = document.getElementById('navbar');
                if (sidebar.classList.contains('active')) {
                    toggleSidebar();
                }
            }
        });

        // Lógica de la Notificación
        function showNotification(message, type) {
            const notification = document.getElementById('notification');
            const messageElement = document.getElementById('notification-message');
            const iconElement = document.getElementById('notification-icon');

            // 1. Limpiar clases de estilo previas y ocultar la notificación
            notification.classList.remove('notification-success', 'notification-error', 'notification-show');

            // 2. Asignar el nuevo mensaje y el tipo
            messageElement.textContent = message;
            if (type === 'success') {
                notification.classList.add('notification-success');
                iconElement.textContent = '✅';
            } else if (type === 'error') {
                notification.classList.add('notification-error');
                iconElement.textContent = '❌';
            }

            // 3. Forzar el recálculo de la página. ESTO ES CLAVE PARA LA ANIMACIÓN.
            notification.offsetWidth;

            // 4. Agregar la clase que dispara la animación de deslizamiento
            notification.classList.add('notification-show');

            // 5. Ocultarla automáticamente después de 3 segundos
            setTimeout(() => {
                notification.classList.remove('notification-show');
            }, 3000);
        }

        // Revisa si hay un mensaje en la URL
        document.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            const success = urlParams.get('success');
            const error = urlParams.get('error');

            if (success) {
                const decodedMessage = success.replace(/\+/g, ' ');
                showNotification(decodedMessage, 'success');
            } else if (error) {
                const decodedMessage = error.replace(/\+/g, ' ');
                showNotification(decodedMessage, 'error');
            }
        });
    </script>
</body>
</html>