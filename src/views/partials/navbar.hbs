<style>
    
        .menu-toggle {
            position: absolute;
            left: 2rem;
            background: transparent;
            border: 2px solid var(--color-white); /* Borde para el botón */
            color: var(--color-white);
            padding: 0.5rem;
            border-radius: 8px;
            cursor: pointer;
            transition: var(--transition);
            font-size: 1.8rem;
        }

        .menu-toggle:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: scale(1.1);
        }

        /* Sidebar desplegable */
        .navbar {
            position: fixed;
            top: 0;
            left: -350px;
            width: 350px;
            height: 100vh;
            background-color: var(--color-card-bg);
            box-shadow: 4px 0 20px rgba(0, 0, 0, 0.1);
            transition: var(--transition);
            z-index: 1000;
            overflow-y: auto;
        }

        .navbar.active {
            left: 0;
        }

        .navbar-header {
            background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
            color: var(--color-white);
            padding: 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 10;
        }

        .navbar-title {
            font-size: 1.4rem;
            margin: 0;
            color: var(--color-white);
            font-weight: 600;
        }

        .sidebar-close {
            background: none;
            border: none;
            color: var(--color-white);
            font-size: 2rem;
            cursor: pointer;
            transition: var(--transition);
        }

        .sidebar-close:hover {
            transform: rotate(90deg);
            color: var(--color-secondary); /* Usa el color secundario */
        }

        .navbar-nav {
            padding: 1.5rem;
        }

        .navbar-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .navbar-item {
            margin-bottom: 1.25rem; /* Más espaciado */
        }

        .navbar-link {
            text-decoration: none;
            color: var(--color-text);
            font-weight: 500;
            display: block;
            padding: 0.75rem 1rem;
            border-radius: 6px;
            transition: background-color 0.3s ease, color 0.3s ease, border-left-color 0.3s ease;
            border-left: 4px solid transparent;
            position: relative;
        }

        .navbar-link::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 0;
            background-color: var(--color-secondary); /* Detalle de color secundario */
            transition: width 0.3s ease;
            z-index: -1;
            opacity: 0.2;
        }

        .navbar-link:hover {
            background-color: var(--color-primary-light);
            color: var(--color-primary-dark);
        }

        .navbar-link:hover::before {
            width: 100%;
        }

        .navbar-link.active {
            color: var(--color-primary-dark);
            background-color: var(--color-primary-light);
            border-left-color: var(--color-primary);
            font-weight: 700;
        }
        
        /* Overlay para cerrar sidebar */
        .sidebar-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            opacity: 0;
            visibility: hidden;
            transition: var(--transition);
            z-index: 999;
        }

        .sidebar-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .navbar {
                width: 300px;
                left: -300px;
            }
            .header {
                padding: 1rem 1.5rem;
            }
            .menu-toggle {
                left: 1rem;
            }
            .main-container {
                padding: 1rem;
                margin: 1rem auto;
            }
            .content {
                padding: 2rem 1.5rem;
            }
            .form-container {
                max-width: 100%;
            }
            .content h2 {
                font-size: 2rem;
            }
            .content::before {
                height: 4px;
            }
        }
</style>

<div class="navbar-nav">
  <ul class="navbar-list">
    <li class="navbar-item"><a
        href="/plantilla"
        class="navbar-link active"
      >Inicio</a></li>

    {{!-- ✅ CATEGORÍAS: Visible para todos (Solo requiere requireAuth) --}}
    <li class="navbar-item"><a 
        href="/categoria/agregar"
        class="navbar-link"
      >Categorías</a></li>
      
    {{!-- ✅ PRODUCTOS: Visible para todos (Solo requiere requireAuth) --}}
    <li class="navbar-item"><a
        href="/producto/agregar"
        class="navbar-link"
      >Productos</a></li>
      
    {{!-- ✅ INVENTARIO: Visible para todos (Solo requiere requireAuth) --}}
    <li class="navbar-item"><a
        href="/inventario/agregar"
        class="navbar-link"
      >Inventario</a></li>
    
    {{!-- Clientes, Ventas, Detalles de Pedido y Proveedores ya estaban visibles --}}
    <li class="navbar-item"><a
        href="/cliente/agregar"
        class="navbar-link"
      >Clientes</a></li>
    <li class="navbar-item"><a
        href="/venta/agregar"
        class="navbar-link"
      >Ventas</a></li>
    <li class="navbar-item"><a
        href="/detalle/agregar"
        class="navbar-link"
      >Detalles de Pedido</a></li>
    <li class="navbar-item"><a
        href="/proveedor/agregar"
        class="navbar-link"
      >Proveedores</a></li>
      
    {{!-- 🔒 ADMINISTRADORES: Solo visible para Administrador --}}
    {{#if user.administrador}}
    <li class="navbar-item"><a
        href="/administrador/agregar"
        class="navbar-link"
      >Administradores</a></li>
    {{/if}}

  </ul>
</div>